LES計算手順
Init 初期条件
↓
時間ループに入る
↓
EXTP　時間発展の計算のためのデータ保存（Uを以下で算出して，U1=U,U2=U1としてデータを保存）
↓
ENERGY　渦粘性MYTの更新　予測値を使う？
↓
KBC　境界条件を設定する　運動方程式のために
↓
NSE　運動方程式を解く　仮速度の算出
↓
SMAC　圧力方程式（式形からすると圧力拡散方程式？）を解いて仮速度を補正
↓
次の時間ループで回す




IJKの位置・流速の対応
I:x方向⇒流下，U
J:y方向⇒鉛直，V
K:z方向⇒横断，W



メモ
BOUNDARY CONDITION 1458これはLES計算用？



・INIT（7108）
格子生成，1階微分演算子，2階微分演算子
LES定数（α，Cs，Ce）→k方程式のため？ENERGYで確認必要！
初期水深と水位の設定（GL:河床高，SODO:粗度係数，Qin:流量，DEP:マニングの等流水深）
流速の設定：対数則
〇1は1つ前の時間のデータ
〇2は2つ前の時間のデータ

出力nohup.outで最初に書かれるRS,RC,DRはそれじれ土粒子密度，流体密度，比重


・EXTP（8669）
〇1は1つ前の時間のデータ
〇2は2つ前の時間のデータ
〇3は3つ前の時間のデータ（Uのみ書かれているけど要らない？，3次ではなく2次？）


・ENERGY（3119）
UKG,UKE,UKD⇒i,j,kの速度ひずみを算出，中心差分　v,wも同様に算出
DETはフィルタ幅？　CK(濃度C)を用いているけど，GALでない限り0？　INITで濃度C(I,J,K)=CSMAX＝0にしているから0？
IGL(I,K)はINITで2としていて，底面を明示
【k方程式関係】
輸送方程式関連は後で考える必要あり
HKは任意点における乱流エネルギー，乱流エネルギーの境界条件は周期境界条件
SKは乱流エネルギーの変化増分，水面で0

【smagorinsky】
SK＝0，HK(I,J,K)=0（乱れエネルギーの輸送は考えない，乱れエネルギーの方程式の変数をリセット）
渦粘性係数MYTを以下の式で算出
MYT(I,J,K)=(0.1*DET(I,J,K))**2.0 &
             *(2.0*( (2.0*UKG(I,J,K))**2+(2.0*VKE(I,J,K))**2 &
                    +(2.0*WKD(I,J,K))**2+(UKE(I,J,K)+VKG(I,J,K))**2 &
                    +(UKD(I,J,K)+WKG(I,J,K))**2 &
                    +(VKD(I,J,K)+WKE(I,J,K))**2 ))**0.50
νt=(CsΔ)^2|S_ij|を算出しており，DETの格子幅が粗すぎるとMYTが大きくなってしまう？

νtが妥当な値ではない，大きくなる⇒粘性が大きくなっている
水面では渦粘性係数が壁よりも弱いから，Csが一定ではおかしい？⇒k方程式の方が適当？，dynamic Smagorinskyモデルとかを導入する必要ありぽい
⇒k方程式を解く方が良い？

GOSAEは乱流エネルギーの勾配SKの計算領域内の総和⇒k方程式の場合にはGOSAEは計算されるが，smagorinskyの場合は0




・KBC
kinematic boundary conditionのことで，水面の境界条件

重要なのは，nonslipか周期境界どうか

下のコロケート格子のやつはどこでつかわれる？
UTは鉛直平均流速？（初期条件では少なくともそう）
ELESとHHの初期条件は以下の通り　HHは等流水深でELESが水面変位？
HH(I,K)=DEP(I,K)
ELES(I,K)=0.0

それに付随して水面関係，SSDが水面変動に関する時間発展の誤差量
ELES(I,K)=ELES(I,K)+SSD(I,K)

!DEPが瞬間水深，ETAが水位？（GLは河床高位置）
DEP(I,K)=HH(I,K)+ELES(I,K)
ETA(I,K)=DEP(I,K)+GL(I,K)

!収束計算における誤差量の算出
ELES1が前の時間の水面変動値
SSD(I,K)=ELES1(I,K)-DT*(DUDX(I,K)+DWDZ(I,K))-ELES(I,K)


!********** COLLOCATE MESH BY SUMO 23/05/25 **********!
            !∂UH/∂x,∂WH/∂zの計算　中心差分　運動方程式の移流項の計算に使われる？
 DUDX(I,K)=((UT(I+1,K)+UT(I,K))*(ELES(I+1,K)+HH(I+1,K)+ELES(I,K)+HH(I,K))*0.25 &
 -(UT(I,K)+UT(I-1,K))*(ELES(I,K)+HH(I,K)+ELES(I-1,K)+HH(I-1,K))*0.25)*GX(I)
 DWDZ(I,K)=((WT(I,K+1)+WT(I,K))*(ELES(I,K+1)+HH(I,K+1)+ELES(I,K)+HH(I,K))*0.25 &
 -(WT(I,K)+WT(I,K-1))*(ELES(I,K)+HH(I,K)+ELES(I,K-1)+HH(I,K-1))*0.25)*DZ(K)

 351    CONTINUE


 LOOP(E)= 21  GOSAE=          NaN
 LOOP(D)=     4  GOSAD=        0.00005
                         LOOP(UVW)= 21  GOSAQ=                 NaN
                           ZANSU=          NaN
 LOOP(P)=   201  GOSAP=            NaN





・データ出力関係
nohup.outは標準出力のデータを保存している
nohup ./run &はログアウトしてもプログラムを止めないためのコマンドで，WRITE(*, ...)またはWRITE(0, ...)のものはこれに出力される
⇒ここの誤差について，理解する必要あり

CPUTIMEは各計算にかかる時間を保存


